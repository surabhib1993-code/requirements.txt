# requirements.txt for site-monitor project
# Minimal set of runtime dependencies required by site_monitor.py.
# Pin or adjust versions as needed for your environment.

requests>=2.28.2
PyYAML>=6.0
python-dateutil>=2.8.2
